// querying functions //
var connection = ('./connection');
var prompt = ('require');

// App prompts user with messages //

var requestProductId;
var requestQuantity;

// Prompt //

prompt.start();
prompt.message = "";